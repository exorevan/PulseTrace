app:
  mode: local
  output_format: console

logging:
  level: INFO

model:
  type: keras
  path: weights/cifar10_mlp.keras

dataset:
  type: builtin
  name: cifar10
  max_samples: 100

explainer:
  type: shap
  num_features: 10

local:
  dataset:
    type: builtin
    name: cifar10
    only_x: true
    max_samples: 1
