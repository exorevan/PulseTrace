app:
  mode: local
  output_format: console

logging:
  level: INFO

model:
  type: sklearn
  path: weights/iris_random_forest.pkl

dataset:
  type: builtin
  name: iris

explainer:
  type: shap
  num_features: 10

local:
  dataset:
    type: builtin
    name: iris
    only_x: true
    max_samples: 1
