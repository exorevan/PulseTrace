app:
  mode: global
  output_format: html

logging:
  level: INFO

model:
  type: keras
  path: weights/mnist_mlp.keras

dataset:
  type: builtin
  name: mnist
  max_samples: 100

explainer:
  type: shap
  num_features: 10
