app:
  mode: global
  output_format: console

logging:
  level: INFO

model:
  type: sklearn
  path: weights/house_lin_reg.pkl

dataset:
  type: csv
  path: datasets/house_pricing_dataset.csv
  delimiter: ","
  header: 0
  only_x: true

explainer:
  type: shap
  num_features: 15
