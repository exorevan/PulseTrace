app:
  mode: global
  output_format: html

logging:
  level: INFO

model:
  type: sklearn
  path: weights/ts_arrow_mlp.pkl

dataset:
  type: timeseries
  path: datasets/ts_arrow_head_train.npy
  n_timesteps: 251

explainer:
  type: shap
  num_features: 10
