app:
  mode: global
  output_format: console

logging:
  level: INFO

model:
  type: keras
  path: weights/cifar10_mlp.keras

dataset:
  type: builtin
  name: cifar10
  max_samples: 100

explainer:
  type: lime
  num_features: 10
  num_samples: 200
  global_samples: 5
