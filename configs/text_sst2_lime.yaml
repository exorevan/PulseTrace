app:
  mode: global
  output_format: html

model:
  type: hf
  path_or_name: "distilbert-base-uncased-finetuned-sst-2-english"
  labels: ["negative", "positive"]

dataset:
  type: text
  path: datasets/sst2_sample

explainer:
  type: lime
  num_features: 10
  global_samples: 5
  num_samples: 300
