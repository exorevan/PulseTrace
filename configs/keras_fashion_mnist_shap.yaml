app:
  mode: global
  output_format: console

logging:
  level: INFO

model:
  type: keras
  path: weights/fashion_mnist_mlp.keras

dataset:
  type: builtin
  name: fashion_mnist
  max_samples: 100

explainer:
  type: shap
  num_features: 10
