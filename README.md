pip install -r requirements.in
pip install -r requirements.dev
pip install -e .

Пример запуска:

```
python main.py --cfg config/config.yaml
```

TODOs:
main.py

models/
explainers/
datasets/
utils/
tests/
